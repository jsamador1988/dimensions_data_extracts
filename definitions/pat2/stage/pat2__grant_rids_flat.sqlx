config {
  type: "table",
  schema: "rd_stage",
  name: "pat2__grant_rids_flat",
  tags: ["pat2", "stage"],
  description: "Grant×researcher_id rows (scalar-only), built from grants_gov_core for RID matching."
}

WITH g AS (
  SELECT
    country_code,
    grant_id,
    start_dt,
    researcher_ids,
    investigators
  FROM ${ref("pat2__grants_gov_core")}
  WHERE start_dt IS NOT NULL
),

rid1 AS (
  SELECT
    country_code,
    grant_id,
    start_dt,
    rid AS researcher_id
  FROM g
  CROSS JOIN UNNEST(IFNULL(researcher_ids, [])) AS rid
  WHERE rid IS NOT NULL
),

rid2 AS (
  SELECT
    country_code,
    grant_id,
    start_dt,
    inv.researcher_id AS researcher_id
  FROM g
  CROSS JOIN UNNEST(IFNULL(investigators, [])) AS inv
  WHERE inv.researcher_id IS NOT NULL
)

SELECT DISTINCT
  country_code,
  grant_id,
  start_dt,
  researcher_id
FROM (
  SELECT * FROM rid1
  UNION ALL
  SELECT * FROM rid2
)
